import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Queue — 다가오는 데이트·기념일 (목업 10) — M4 구현 */
export default function Queue() {
  return <PlaceholderScreen title="다음 순서" note="M4: upcoming tracks + 기념일" />;
}
