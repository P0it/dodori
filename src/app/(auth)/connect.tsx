import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** 커플 연결 선택 (목업 02 ConnectChoice) — M1 구현 */
export default function Connect() {
  return <PlaceholderScreen title="커플 연결" note="M1: 초대 보내기 / 코드 입력 분기" />;
}
