import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** 재생 뷰 — 사진 슬라이드쇼 (목업 14) — M3 구현 */
export default function Player() {
  return <PlaceholderScreen title="슬라이드쇼" note="M3: 사진 슬라이드쇼 재생" />;
}
