import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** 내 일정 추가 — 3탭 이내 입력, 제목 숨김 토글 (목업 21) — M2 구현 */
export default function AddEvent() {
  return <PlaceholderScreen title="내 일정" note="M2: events insert + title_hidden" />;
}
