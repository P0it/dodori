import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** 데이트 만들기 위저드 — 날짜 → 장소 (목업 22~23) — M3 구현 */
export default function CreateTrack() {
  return <PlaceholderScreen title="데이트 만들기" note="M3: 날짜 선택 → 장소 담기" />;
}
